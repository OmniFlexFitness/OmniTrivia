# Git files
.git
.gitignore

# Node modules (if any were installed locally)
node_modules

# Docker files
Dockerfile
.dockerignore

# Deployment instructions
DEPLOYMENT.md

# Build output (regenerated inside the image)
dist

# Local secrets - the key is passed via --build-arg instead
.env
