node_modules/
dist/
.env
.env.local
*.local
*.tsbuildinfo

# Firebase emulator scratch
# (.firebaserc is committed: it only names the project, which is public, and
#  committing it is what lets `npm run rules:deploy` work without `use --add`.)
*-debug.log
.firebase/

# Cloudflare Worker local secrets
worker/.dev.vars
.wrangler/
